# FieldLedger v0.3.2

Patch release for theme controls and top-bar cleanup.

## Included

- New persistent `Light` and `AMOLED Dark` modes
- Theme toggle in Settings
- AMOLED dark palette switched to true black surfaces
- Simplified app header so it no longer gets in the way while scrolling

## Testing Notes

- This release asset is a debug APK for manual device testing
- Functionality is unchanged apart from theme selection and header behavior

## APK

- File: `app-debug.apk`
- Package: `com.indie.shiftledger`
